// Using template literals 

// Declaring Variables
let name = "Israel";
let age = 28;
let city = "Kwara";
let appName = "EaseRide"

// Create a template literal that includes these variables
let message = `Hello, ${name}. Welcome to ${appName}, your current age is ${age} years old and you live in live in ${city}.`;

// Print the template literal
console.log(message);

// Interpolate variables into the template using ${}
// Declaring more variables
let firstName = "Israel";
let lastName = "Ayinde";
let jobRole = "Web Developer";

let introduction = `My name is ${firstName} ${lastName}. I am a ${jobRole}.`;

console.log(introduction);