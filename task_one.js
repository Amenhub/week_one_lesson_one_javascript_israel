// creating a variable
let name = "Israel";
let gender = "male";

// creating string concatenation for new string
let welcome = "Hello, " + name + ", " + "welcome to the X-Ride App";

//Output: welcome message
console.log(welcome);

//converting the name to uppercase
let upperCaseName = name.toUpperCase ();
console.log(upperCaseName);

//converting the name to lowercase
let lowerCaseName = name.toLowerCase ();
console.log(lowerCaseName);

// Slicing the 'name' to extract part of it
let slicedName = name.slice(0, 3); 
console.log(slicedName); 